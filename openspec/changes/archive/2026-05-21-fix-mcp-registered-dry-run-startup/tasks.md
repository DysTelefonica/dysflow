# Tasks: MCP Registered Dry-run after Startup Config Failure

## Strict TDD

- [x] RED: MCP startup outside repo returns startup config error for registered context dry-run.
- [x] GREEN: unavailable startup still wires legacy import service capable of per-call registry resolution.
- [x] Verify: `pnpm test && pnpm build`.
