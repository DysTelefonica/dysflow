Closes #380

### PR Type
- [x] Code refactoring (type:refactor)

### Summary
- Extract and unify Access COM spawn/teardown and WMI-based process enumeration logic into a reusable shared module `scripts/lib/dysflow-access-com.ps1`.
- Clean up duplicate definitions in `dysflow-access-runner.ps1` and `dysflow-vba-manager.ps1` to prevent code drift and improve maintainability.
- Add robust behavioral tests for Close-CanonicalAccess and process enumeration to prevent zombie processes.

### Changes Table
| File | Change |
|------|--------|
| `scripts/lib/dysflow-access-com.ps1` | New shared PowerShell module containing Open-CanonicalAccess, Close-CanonicalAccess, and Get-MsAccessProcessesBounded. |
| `scripts/tests/dysflow-access-com.Tests.ps1` | New behavioral unit tests for the shared COM and WMI functions. |
| `scripts/dysflow-access-runner.ps1` | Dot-source the shared module, delegate COM setup/teardown, and remove duplicate local functions. |
| `scripts/dysflow-vba-manager.ps1` | Dot-source the shared module, delegate close paths, and remove duplicate local functions. |
| `scripts/tests/dysflow-access-runner.Tests.ps1` | Update test setup and assertions to use the shared module, and add tests for Null-PID fallback paths. |
| `scripts/tests/dysflow-vba-manager.Tests.ps1` | Update test setup and assertions to use the shared module. |
| `test/scripts-vba-manager.test.ts` | Refactor vitest assertions to check shared module presence and dispatcher wiring. |

### Test Plan
- Run Pester tests: `pnpm test:ps1` (200 passed)
- Run Vitest suite: `pnpm test` (845 passed)
- Manually check execution and confirm zero lingering MSACCESS.EXE processes.

### Contributor Checklist
- [x] Linked an approved issue
- [x] Added exactly one `type:*` label
- [x] Ran shellcheck on modified scripts
- [x] Skills tested in at least one agent
- [x] Docs updated if behavior changed
- [x] Conventional commit format
- [x] No `Co-Authored-By` trailers
