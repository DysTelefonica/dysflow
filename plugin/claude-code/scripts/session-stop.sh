#!/usr/bin/env bash
# =====================================================================
# dysflow-plugin:session-stop v2.31.0
# =====================================================================
# This file is GENERATED by the dysflow plugin. DO NOT EDIT MANUALLY.
# On plugin update, this file is replaced.
# =====================================================================
# Fires on Stop event. Surfaces drift via dysflow doctor before the
# session ends, so the next session has a clean handoff.
# Coexists with engram's session-stop — both fire independently.
# =====================================================================
set -euo pipefail

if command -v dysflow >/dev/null 2>&1; then
    echo "[dysflow-plugin v2.31.0] Running doctor to surface drift..."
    dysflow doctor 2>/dev/null || true
fi

exit 0
