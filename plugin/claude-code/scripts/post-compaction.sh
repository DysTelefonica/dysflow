#!/usr/bin/env bash
# =====================================================================
# dysflow-plugin:post-compaction v2.31.0
# =====================================================================
# This file is GENERATED by the dysflow plugin. DO NOT EDIT MANUALLY.
# On plugin update, this file is replaced.
# =====================================================================
# Fires on SessionStart with matcher "compact". Re-establishes dysflow
# context because the agent's working memory was just compacted.
#
# This is THE hook that prevents the agent from "going crazy" after
# compaction: it re-injects the canonical workflow + live project state
# before the agent continues.
# =====================================================================
set -euo pipefail

# Re-surface the canonical loop (truncated — full text is in skills/dysflow-protocol).
cat <<'EOF'
[dysflow-plugin v2.31.0] Re-establishing context after compaction.

Reminders (full rules in skills/dysflow-protocol/SKILL.md):
  - 8-step canonical loop (get_capabilities -> doctor -> check_id -> policy -> plan -> ask -> commit -> verify)
  - NEVER: dryRun: true, kill MSACCESS, write to production, claim TDD-green without compile
  - apply: false for plan; apply: true for commit
  - confirmedRequiresConfirmation: true when implements_check.requires_confirmation
EOF

# Re-run dysflow state to refresh live project context.
if command -v dysflow >/dev/null 2>&1; then
    dysflow state 2>/dev/null || echo "[dysflow-plugin] dysflow state unavailable"
fi

exit 0
